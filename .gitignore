# Ignore node_modules directory
# This directory contains all the npm packages. It can be regenerated by running 'npm install'.
node_modules

# Ignore database data directory
# This directory contains database files that are generated and managed by the database system.
# They are not needed in version control.
/database/data